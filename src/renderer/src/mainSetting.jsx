// src/Setting.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.scss'
import Setting from './Setting'

ReactDOM.createRoot(document.getElementById('setting')).render(
    <React.StrictMode>
        <Setting />
    </React.StrictMode>
)
